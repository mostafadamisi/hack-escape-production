{
  "name": "hack-escape-orchestrator",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "deploy:railway": "echo 'Railway will use the root Dockerfile'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
